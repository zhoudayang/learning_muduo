cmake_minimum_required(VERSION 3.5)
project(async_logging)
include_directories(${CMAKE_SOURCE_DIR})
set(src LogStream.cpp logging.cpp LogFile.cpp)
add_library(log SHARED ${src})
target_link_libraries(log thread time)
